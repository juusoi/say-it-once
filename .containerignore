# Only the four files the browser actually loads belong in the image.
.git
.github
test
deploy/say-it-once.container
# **/*.md rather than *.md: the latter matches only the repo root, so every doc
# added under deploy/ would otherwise have to be listed here by hand -- and a
# missed one lands in the published image without anything complaining.
**/*.md
